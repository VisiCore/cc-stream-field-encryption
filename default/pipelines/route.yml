id: default
routes:
  - id: encrypt_pii
    name: encrypt_pii
    final: true
    disabled: false
    pipeline: encrypt_pii
    description: Every event entering the pack goes through the field-encryption
      pipeline, then __group hands it to the Worker Group route table with
      __inputId intact. Route it to a destination Cribl Search can read there.
    enableOutputExpression: false
    filter: "true"
    clones: []
    output: __group
  - id: default
    name: default
    final: true
    disabled: false
    pipeline: main
    description: ""
    enableOutputExpression: false
    filter: "true"
    clones: []
    output: default
